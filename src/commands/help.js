module.exports = {
    name: "help",
    usage: "help {options}",
    alias: ["commands"],
    cooldown: 3,
    onlyowner: false,
    onlydev: false,
    perms: [],
    run: (client, message, args, storage) => {

      message.channel.send(
        "a"
      )

    }
  }
